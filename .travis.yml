language: objective-c

# if test available use this config istead of a custom script
#xcode_destination: platform=iOS Simulator,OS=11.3,name=iPhone X
#xcode_project: TBGameProto.xcodeproj 
# or use 
# xcode_workspace: TBGameProto.xcworkspace 

#xcode_scheme: TBGameProto

script:
  - xcodebuild -scheme TBGameProto -workspace TBGameProto.xcworkspace -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 6S,OS=12.1' build